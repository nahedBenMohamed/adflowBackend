//TODO Make fields naming in plural and consistent with such frontend enum, sort descending
export enum EntityEventFilter {
  All = 'all',
  Activities = 'activities',
  Calls = 'calls',
  Documents = 'documents', //TODO: remove
  Files = 'files',
  Notes = 'notes',
  Mail = 'mail',
  Orders = 'orders',
  Shipments = 'shipments',
  Tasks = 'tasks',
}
